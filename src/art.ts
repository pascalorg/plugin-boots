import bootsIcon from './assets/boots-icon.webp'

/** The panel / plugin icon — a work boot on a blueprint. Next static image
 * imports are `{ src, width, height }`; hosts want the URL string. */
export const BOOTS_ICON: string = bootsIcon.src
